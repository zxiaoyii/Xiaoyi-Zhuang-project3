# Class / Render: use MONGODB_URI in the host’s Environment. Local: copy to .env (never commit .env).
MONGODB_URI=mongodb://127.0.0.1:27017/sudokuapp
# Optional alias if you prefer
# MONGODB_URL=mongodb://127.0.0.1:27017/sudokuapp
COOKIE_SECRET=change-this-to-a-long-random-string
PORT=8000
